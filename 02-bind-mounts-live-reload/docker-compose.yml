services:
  fastapi-app:
    build: .
    container_name: fastapi-bind-mounts
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      APP_ENV: development
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
